/// An endpoint designed to listen for unsolicited messages
///
/// This is for receiving events or creating a local API so remote modules can manage the data

#[derive(Debug, Clone)]
pub struct ListenerEndpoint {}
